/** @type {import('next').NextConfig} */
const nextConfig = {
  // Every route is statically generated, so the site can also be exported as
  // plain files with `output: 'export'` if you'd rather host it somewhere
  // without a Node runtime.
};

export default nextConfig;
